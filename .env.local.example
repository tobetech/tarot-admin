# คัดลอกไฟล์นี้เป็น .env.local (สำหรับรันในเครื่อง) แล้วใส่ค่าจริงจาก Supabase Dashboard
# Project Settings -> API

NEXT_PUBLIC_SUPABASE_URL=https://xxxxxxxxxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOi...

# ตอน deploy จริงบน Vercel ให้ไปตั้งค่า 2 ตัวนี้ใน
# Vercel Dashboard -> Project -> Settings -> Environment Variables แทน
# (ไม่ต้อง commit ไฟล์ .env.local เข้า git)
